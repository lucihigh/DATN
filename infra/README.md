# Infra

Docker compose for local dev:

`
docker compose up -d
`

Services:
- Postgres 15 (5432)
- API (4000)
- Web (5173)
- AI service (8000)

Init SQL runs from init-db.sql for enabling extensions (placeholder).
